java minijavac.graph.TestGraph2
Before~~~~~~~~~~


herp:
LOOP:  param a
       param b
       x := call f, 2
       c := c + a
       iftrue a goto LOOP
       return c



After~~~~~~~~


herp:
LOOP:  $a0 := $t0
       $a1 := $t2
       $a2 := 2
       x := call f, 2
       $t1 := $t1 + $t0
       iftrue $t0 goto LOOP
       return $t1

